#!/usr/bin/python3
def replace_in_list(my_list, idx, element):
    num = len(my_list) - 1
    if idx < 0 or idx > num:
        print("{}" .format(my_list))
    else:
        mylist.insert(idx, element)
        print"{}" .format(my_list))
